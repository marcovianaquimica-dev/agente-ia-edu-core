.env

.venv/

# Python
__pycache__/
*.py[cod]

# Python package metadata
*.egg-info/

# Locally acquired official source documents, plus ephemeral audit/perf
# reports and logs generated during acceptance walks and manual scripts.
var/

# Local env backups and ad-hoc debug scripts (never meant for the repo).
.env.save
debug_http.py
debug_http_verbose.py
debug_isolation.py
scratchpad_*.txt

# Ephemeral fixture output regenerated on each test run.
tests/_p10tmp/mf_*.json

# Worktrees isolados criados por sessão (nunca versionados).
.claude/worktrees/
